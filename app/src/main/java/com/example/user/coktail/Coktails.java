package com.example.user.coktail;

import android.app.Application;
import android.view.View;
import android.widget.AdapterView;
import android.widget.ArrayAdapter;
import android.widget.ListView;
import android.widget.Toast;

import java.util.ArrayList;

/**
 * Created by user on 10-Jan-18.
 */

public class Coktails extends Application{


/*            try {
        result = data.getCoktails();
    }catch (Exception e){
        Toast.makeText(MainActivity.this,"Error in getCoktails",Toast.LENGTH_SHORT).show();
    }
        if(result!=null){
        ArrayAdapter<String> items=new ArrayAdapter<String>(this,android.R.layout.simple_list_item_1,result);
        lst.setAdapter(items);
    }

        else{
        Toast.makeText(MainActivity.this,"Error",Toast.LENGTH_SHORT).show();
    }


    AddListenerOnList();

}
    public void AddListenerOnList(){
        ListView lst=(ListView)findViewById(R.id.listView);
        lst.setOnItemClickListener(new AdapterView.OnItemClickListener() {
                                       public void onItemClick(AdapterView<?> parentAdapter, View view, int position,
                                                               long id) {


                                       }
                                   }


        );
    }*/
}
